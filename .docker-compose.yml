version: "3.13"

services:
  api:
    image: asadullahshehbaz/fastapi-research-assistant:latest
    container_name: fastapi-api
    ports:
      - "8000:8000"
    volumes:
      - data_volume:/app/data
    env_file:
      - .env
    restart: always

volumes:
  data_volume:
